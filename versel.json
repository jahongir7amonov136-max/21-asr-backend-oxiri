{
  "devCommand": "uvicorn server:app --reload",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "server.py"
    }
  ],
  "functions": {
    "server.py": {
      "runtime": "vercel-python@3.10"
    }
  }
}